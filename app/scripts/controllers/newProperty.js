'use strict';

/**
 * @ngdoc function
 * @name realEstateApp.controller:newProperty
 * @description
 * # newProperty
 * Controller of the realEstateApp
 */
app.controller('newProperty', function ($rootScope) {
  $rootScope.pageTitle = 'Add New Property';
});
