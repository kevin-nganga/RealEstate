'use strict';

/**
 * @ngdoc function
 * @name realEstateApp.controller:profile
 * @description
 * # profile
 * Controller of the realEstateApp
 */
app.controller('profile', function ($rootScope) {
  $rootScope.pageTitle = 'Account Settings';
  });
