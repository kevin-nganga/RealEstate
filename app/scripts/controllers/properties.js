'use strict';

/**
 * @ngdoc function
 * @name realEstateApp.controller:properties
 * @description
 * # properties
 * Controller of the realEstateApp
 */
app.controller('properties', function ($rootScope) {
  $rootScope.pageTitle = 'Dashboard';
});
